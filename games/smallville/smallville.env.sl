// Smallville with the library's defaults. Anything you add here changes
// just that: fields replace the library's, a building or resident with the
// same name merges into it, and `remove building "Name"` drops one.
import smallville

environment Smallville {
  days: 2
}
